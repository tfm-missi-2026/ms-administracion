<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pe.unir.tfm.srp.administracion.repository.CatalogoMapper">

    <sql id="selectAll">
        SELECT id, grupo, id_opcion, opcion,
               estado, fecha_creacion, usuario_creacion,
               fecha_modificacion, usuario_modificacion,
               fecha_eliminacion, usuario_eliminacion, motivo_eliminacion
          FROM msa_catalogo
    </sql>

    <select id="findById" parameterType="java.util.UUID" resultType="pe.unir.tfm.srp.administracion.model.Catalogo">
        <include refid="selectAll"/>
        WHERE id = #{id} AND estado = 1
    </select>

    <select id="listByGroup" parameterType="string" resultType="pe.unir.tfm.srp.administracion.model.Catalogo">
        <include refid="selectAll"/>
        WHERE grupo = #{grupo} AND estado = 1
        ORDER BY id_opcion
    </select>

    <select id="listActive" resultType="pe.unir.tfm.srp.administracion.model.Catalogo">
        <include refid="selectAll"/>
        WHERE estado = 1
        ORDER BY grupo, id_opcion
    </select>

    <select id="countByGroupAndOptionId" resultType="int">
        SELECT COUNT(*) FROM msa_catalogo
         WHERE grupo = #{grupo} AND id_opcion = #{idOpcion} AND estado = 1
    </select>

    <insert id="insert" parameterType="pe.unir.tfm.srp.administracion.model.Catalogo">
        INSERT INTO msa_catalogo (id, grupo, id_opcion, opcion, estado, fecha_creacion, usuario_creacion)
        VALUES (#{id}, #{grupo}, #{idOpcion}, #{opcion}, 1, NOW(), #{usuarioCreacion})
    </insert>

    <update id="update" parameterType="pe.unir.tfm.srp.administracion.model.Catalogo">
        UPDATE msa_catalogo
           SET grupo = #{grupo},
               id_opcion = #{idOpcion},
               opcion = #{opcion},
               fecha_modificacion = NOW(),
               usuario_modificacion = #{usuarioModificacion}
         WHERE id = #{id} AND estado = 1
    </update>

    <update id="softDelete">
        UPDATE msa_catalogo
           SET fecha_eliminacion = NOW(),
               usuario_eliminacion = #{usuarioEliminacion},
               motivo_eliminacion = #{motivoEliminacion},
               estado = 0
         WHERE id = #{id} AND estado = 1
    </update>

</mapper>